#!/bin/sh

# Choose language: en for English, ru for Russian
# Выберите язык: en для английского, ru для русского
echo "Choose language (en/ru):"
read -r LANG

if [ "$LANG" = "ru" ]; then
    SUCCESS_MSG="✅ Успешно"
    ERROR_MSG="❌ Ошибка"
    WARNING_MSG="⚠️ Предупреждение"
    INFO_MSG="ℹ️ Информация"
    AVERAGE_MSG="📈 Среднее время ответа для"
    BEST_SERVER_MSG="🏆 Лучший сервер:"
else
    SUCCESS_MSG="✅ Success"
    ERROR_MSG="❌ Error"
    WARNING_MSG="⚠️ Warning"
    INFO_MSG="ℹ️ Info"
    AVERAGE_MSG="📈 Average response time for"
    BEST_SERVER_MSG="🏆 Best server:"
fi

# Enable debug mode (can be disabled by removing or commenting out the next line)
# Включаем отладочный режим (можно отключить, удалив или закомментировав следующую строку)
#set -x

# Define color variables
# Определение цветовых переменных
RED='\033[0;31m'      # Red / Красный
GREEN='\033[0;32m'    # Green / Зелёный
YELLOW='\033[0;33m'   # Yellow / Жёлтый
BLUE='\033[38;5;208m' # Orange / Оранж
NC='\033[0m'          # No Color / Без Цвета

# Files and paths
# Файлы и пути
SERVER_LIST="/etc/init.d/servers.txt"
CONFIG_FILE="/etc/sing-box/config.json"
SERVER_PARAMS_FILE="/etc/init.d/servers_config.json"
LOG_FILE="/tmp/ping_log.txt"

# Functions for colored output
# Функции для цветного вывода
info() {
    echo -e "${BLUE}$1${NC}" | tee -a "$LOG_FILE"
}

success() {
    echo -e "${GREEN}$1${NC}" | tee -a "$LOG_FILE"
}

warning() {
    echo -e "${YELLOW}$1${NC}" | tee -a "$LOG_FILE"
}

error() {
    echo -e "${RED}$1${NC}" | tee -a "$LOG_FILE"
}

start() {
    info "$INFO_MSG Checking server availability..."
    info "$INFO_MSG Проверка доступности серверов..."

    # Check if necessary files exist
    # Проверка существования необходимых файлов
    if [ ! -f "$SERVER_LIST" ]; then
        error "$ERROR_MSG Server list file $SERVER_LIST not found."
        error "$ERROR_MSG Файл со списком серверов $SERVER_LIST не найден."
        exit 1
    fi

    if [ ! -f "$SERVER_PARAMS_FILE" ]; then
        error "$ERROR_MSG Server parameters file $SERVER_PARAMS_FILE not found."
        error "$ERROR_MSG Файл с параметрами серверов $SERVER_PARAMS_FILE не найден."
        exit 1
    fi

    best_server=""
    best_average=9999 # Initial high value for comparison / Начальное высокое значение для сравнения

    # Initialize total time and success count variables for each server
    # Инициализируем общую переменную времени и счётчик успешных пингов по каждому серверу
    while read -r server; do
        info "📡 Pinging server: $server..."
        info "📡 Пингуем сервер: $server..."
        total_time=0
        success_count=0

        for i in $(seq 1 10); do
            # Perform ping and extract the response time line
            # Выполняем пинг и извлекаем строку с временем ответа
            ping_output=$(ping -c 1 -W 1 "$server" | grep 'time=')
            if [ -n "$ping_output" ]; then
                # Extract the response time value
                # Извлекаем значение времени ответа
                time=$(echo "$ping_output" | awk -F'time=' '{print $2}' | awk '{print $1}')

                # Check if response time is a number
                # Проверяем, что время ответа является числом
                if echo "$time" | grep -E '^[0-9.]+$' >/dev/null 2>&1; then
                    # Add time to the total sum
                    # Добавляем время к общей сумме
                    total_time=$(awk "BEGIN {printf \"%.2f\", $total_time + $time}")
                    success_count=$((success_count + 1))
                    success "$SUCCESS_MSG Attempt $i: $time ms"
                    success "$SUCCESS_MSG Попытка $i: $time мс"
                else
                    warning "$WARNING_MSG Attempt $i: Invalid response time."
                    warning "$WARNING_MSG Попытка $i: Некорректное время ответа."
                fi
            else
                warning "$WARNING_MSG Attempt $i: Ping failed."

                warning "$WARNING_MSG Попытка $i: Пинг не удался."
            fi
        done

        if [ "$success_count" -gt 0 ]; then
            # Debug information
            # Отладочная информация
            info "📊 total_time: $total_time"
            info "🔢 success_count: $success_count"

            # Calculate average response time
            # Вычисляем среднее время ответа
            average_time=$(awk "BEGIN {printf \"%.2f\", $total_time / $success_count}")
            success "$AVERAGE_MSG $server: $average_time ms (Successful pings: $success_count)"
            success "$AVERAGE_MSG $server: $average_time мс (Успешных пингов: $success_count)"

            # Compare the average time with the current best
            # Сравниваем среднее время с текущим лучшим
            better=$(echo "$average_time < $best_average" | bc)
            if [ "$better" -eq 1 ]; then
                best_average=$average_time
                best_server=$server
                success "🌟 New best server: $best_server with time $best_average ms"
                success "🌟 Новый лучший сервер: $best_server со временем $best_average мс"
            fi
        else
            warning "$WARNING_MSG Server $server is unreachable after 10 attempts."
            warning "$WARNING_MSG Сервер $server недоступен после 10 попыток."
        fi

    done < "$SERVER_LIST"

    if [ -n "$best_server" ]; then
        success "$BEST_SERVER_MSG $best_server with average response time $best_average ms"
        success "$BEST_SERVER_MSG $best_server со средним временем ответа $best_average мс"

        # Get parameters for the best server
        # Получаем параметры для лучшего сервера
        server_params=$(jq ".servers[] | select(.server == \"$best_server\")" "$SERVER_PARAMS_FILE")

        # Debug output
        # Отладочный вывод
        info "📄 Parameters of the selected server:"
        info "📄 Параметры выбранного сервера:"
        echo "$server_params" | tee -a "$LOG_FILE"

        # Check if server_params contains valid JSON
        # Проверяем, что server_params содержит валидный JSON
        if ! echo "$server_params" | jq empty > /dev/null 2>&1; then
            error "$ERROR_MSG Parameters for server $best_server contain invalid JSON."
            error "$ERROR_MSG Параметры сервера $best_server содержат некорректный JSON."
            exit 1
        fi

        if [ -z "$server_params" ]; then
            error "$ERROR_MSG Parameters for server $best_server not found."
            error "$ERROR_MSG Параметры для сервера $best_server не найдены."
            exit 1
        fi
